# كيفية حذف جميع البيانات السابقة من قاعدة البيانات

## 🗑️ الطرق المختلفة لحذف البيانات

### الطريقة الأولى: حذف جميع البيانات وإعادة إنشاء الجداول (الأفضل)

هذه الطريقة تحذف جميع البيانات وتعيد إنشاء الجداول من الصفر:

```bash
php artisan migrate:fresh --seed
```

**ماذا يفعل هذا الأمر:**
- ✅ يحذف جميع الجداول
- ✅ يعيد إنشاء جميع الجداول من جديد
- ✅ يشغل Seeder تلقائياً بعد الإنشاء

**⚠️ تحذير:** هذا الأمر سيحذف **جميع** البيانات بشكل نهائي!

---

### الطريقة الثانية: حذف البيانات فقط (بدون إعادة إنشاء الجداول)

إذا كنت تريد حذف البيانات فقط مع الحفاظ على بنية الجداول:

```bash
php artisan migrate:fresh
php artisan db:seed --class=FakerDataSeeder
```

أو:

```bash
php artisan migrate:fresh
php artisan db:seed
```

---

### الطريقة الثالثة: حذف البيانات يدوياً من phpMyAdmin

1. افتح phpMyAdmin: http://localhost/phpmyadmin
2. اختر قاعدة البيانات `inventory`
3. اختر جميع الجداول
4. اختر "Empty" (تفريغ) من القائمة المنسدلة
5. اضغط "Go" (تنفيذ)

**أو حذف قاعدة البيانات بالكامل:**

1. اختر قاعدة البيانات `inventory`
2. اضغط على "Operations" (عمليات)
3. اضغط "Drop the database" (حذف قاعدة البيانات)
4. أنشئ قاعدة بيانات جديدة باسم `inventory`
5. شغّل:
   ```bash
   php artisan migrate
   php artisan db:seed --class=FakerDataSeeder
   ```

---

### الطريقة الرابعة: استخدام Tinker (لحذف بيانات محددة)

```bash
php artisan tinker
```

ثم في Tinker:

```php
// حذف جميع المنتجات
\App\Models\Product::truncate();

// حذف جميع الفئات
\App\Models\Category::truncate();

// حذف جميع المستخدمين (عدا الإداري)
\App\Models\User::where('type', 'driver')->delete();

// حذف جميع المبيعات
\App\Models\Sale::truncate();
\App\Models\SaleItem::truncate();

// حذف جميع المخزون
\App\Models\WarehouseStock::truncate();
\App\Models\DriverStock::truncate();
```

---

## 📋 الخطوات الموصى بها (الطريقة الأسهل)

### 1. احذف جميع البيانات وأعد إنشاءها:

```bash
php artisan migrate:fresh --seed
```

هذا الأمر سيقوم بـ:
- حذف جميع الجداول
- إعادة إنشاءها
- ملء البيانات الجديدة تلقائياً

---

### 2. إذا أردت التحكم أكثر:

```bash
# حذف وإعادة إنشاء الجداول
php artisan migrate:fresh

# ملء البيانات
php artisan db:seed --class=FakerDataSeeder
```

---

## ⚠️ تحذيرات مهمة

1. **احفظ نسخة احتياطية** إذا كانت لديك بيانات مهمة
2. **تأكد من إغلاق التطبيق** قبل الحذف
3. **لا تستخدم في الإنتاج** (Production) بدون نسخة احتياطية

---

## 🔄 سيناريوهات الاستخدام

### السيناريو الأول: إعادة تعيين كامل

```bash
php artisan migrate:fresh --seed
```

### السيناريو الثاني: حذف البيانات فقط (بدون إعادة إنشاء الجداول)

```bash
php artisan migrate:fresh
php artisan db:seed --class=FakerDataSeeder
```

### السيناريو الثالث: حذف بيانات محددة فقط

استخدم Tinker كما هو موضح أعلاه

---

## ✅ التحقق من الحذف

بعد الحذف، تحقق من:

1. **من phpMyAdmin:**
   - افتح قاعدة البيانات `inventory`
   - تأكد من أن الجداول فارغة أو تم حذفها

2. **من Terminal:**
   ```bash
   php artisan tinker
   ```
   ثم:
   ```php
   \App\Models\Product::count(); // يجب أن يكون 0
   \App\Models\User::count(); // يجب أن يكون 0 أو 1 (الإداري فقط)
   ```

---

## 🎯 الأمر الموصى به

**لحذف جميع البيانات وإعادة ملءها:**

```bash
php artisan migrate:fresh --seed
```

هذا هو **أسهل وأسرع** طريقة!

---

**ملاحظة:** جميع الأوامر يجب تنفيذها من مجلد المشروع (`inventory+backend`)

