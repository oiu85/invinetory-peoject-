# تعليمات استيراد Postman Collection

## 📦 الملفات المتوفرة

1. **Inventory_Backend.postman_collection.json** - مجموعة API كاملة
2. **Inventory_Backend_Environment.postman_environment.json** - متغيرات البيئة

## 🚀 خطوات الاستيراد

### الخطوة 1: استيراد Collection

1. افتح Postman
2. اضغط على **Import** (أعلى يسار الشاشة)
3. اختر **File** أو **Upload Files**
4. اختر ملف: `postman/Inventory_Backend.postman_collection.json`
5. اضغط **Import**

### الخطوة 2: استيراد Environment

1. في Postman، اضغط على **Environments** (أعلى يمين الشاشة)
2. اضغط على **Import**
3. اختر ملف: `postman/Inventory_Backend_Environment.postman_environment.json`
4. اضغط **Import**

### الخطوة 3: تفعيل Environment

1. في أعلى يمين Postman، اختر Environment: **Inventory Backend - Local**
2. تأكد من أنه مفعّل

## 📋 المتغيرات المتوفرة

### متغيرات أساسية:
- `base_url` - رابط API الأساسي (افتراضي: http://localhost:8000)
- `auth_token` - رمز المصادقة (يتم تعيينه تلقائياً بعد تسجيل الدخول)
- `user_id` - معرف المستخدم (يتم تعيينه تلقائياً)
- `user_type` - نوع المستخدم (admin/driver)

### متغيرات للاختبار:
- `product_id` - معرف المنتج (افتراضي: 1)
- `driver_id` - معرف السائق (افتراضي: 2)
- `sale_id` - معرف عملية البيع (افتراضي: 1)

### بيانات تسجيل الدخول:
- `admin_email` - بريد الإداري
- `admin_password` - كلمة مرور الإداري
- `driver_email` - بريد السائق
- `driver_password` - كلمة مرور السائق

## 🔐 كيفية الاستخدام

### 1. تسجيل الدخول أولاً:

1. افتح **Authentication > Login**
2. تأكد من أن Environment مفعّل
3. اضغط **Send**
4. سيتم حفظ `auth_token` تلقائياً في Environment

### 2. استخدام باقي الـ Endpoints:

- جميع الـ Endpoints تستخدم `{{auth_token}}` تلقائياً
- يمكنك تغيير `{{product_id}}`, `{{driver_id}}`, `{{sale_id}}` حسب الحاجة

## 📁 المجموعات المتوفرة

### 1. Authentication
- Login (تسجيل الدخول)
- Logout (تسجيل الخروج)
- Get Current User (الحصول على المستخدم الحالي)

### 2. Categories
- Get All Categories (جميع الفئات)
- Create Category (إنشاء فئة)

### 3. Products
- Get All Products (جميع المنتجات)
- Create Product (إنشاء منتج)
- Update Product (تحديث منتج)
- Delete Product (حذف منتج)

### 4. Warehouse Stock
- Get All Warehouse Stock (مخزون المستودع)
- Update Warehouse Stock (تحديث المخزون)

### 5. Driver Stock
- Get Driver Stock (مخزون السائق)
- Assign Stock to Driver (توزيع المخزون على السائق)

### 6. Sales
- Create Sale (إنشاء عملية بيع)
- Get Sale Details (تفاصيل عملية البيع)
- Get Sale Invoice PDF (تحميل الفاتورة PDF)

### 7. Admin Stats
- Get Dashboard Stats (إحصائيات لوحة التحكم)

### 8. Health Check
- Health Check (فحص حالة API)

## 🔄 تحديث المتغيرات

### تحديث product_id:
1. بعد إنشاء منتج جديد، انسخ الـ ID من الـ Response
2. في Environment، غيّر قيمة `product_id`

### تحديث driver_id:
1. بعد تسجيل الدخول كسائق، انسخ الـ ID
2. في Environment، غيّر قيمة `driver_id`

### تحديث sale_id:
1. بعد إنشاء عملية بيع، انسخ الـ ID
2. في Environment، غيّر قيمة `sale_id`

## ⚙️ إعدادات إضافية

### تغيير base_url:
إذا كان الـ API يعمل على منفذ آخر:
1. افتح Environment
2. غيّر قيمة `base_url` إلى المنفذ الجديد
3. مثال: `http://localhost:8080`

### استخدام form-data:
جميع الـ Requests التي تحتاج إرسال بيانات تستخدم **form-data** تلقائياً.

## 🧪 أمثلة على الاستخدام

### مثال 1: تسجيل الدخول كإداري
1. افتح **Authentication > Login**
2. تأكد من البيانات:
   - email: `admin@inventory.com`
   - password: `password`
3. اضغط **Send**
4. سيتم حفظ Token تلقائياً

### مثال 2: إنشاء منتج جديد
1. تأكد من تسجيل الدخول كإداري
2. افتح **Products > Create Product**
3. املأ البيانات:
   - name: `كيس تسوق كبير`
   - price: `2.50`
   - category_id: `1`
   - description: `كيس تسوق بلاستيكي`
   - image: رابط صورة
4. اضغط **Send**

### مثال 3: إنشاء عملية بيع (كسائق)
1. سجّل الدخول كسائق أولاً
2. افتح **Sales > Create Sale**
3. استخدم JSON:
```json
{
    "customer_name": "عبدالرحمن أحمد",
    "items": [
        {
            "product_id": 1,
            "quantity": 10
        }
    ]
}
```
4. اضغط **Send**

## 📝 ملاحظات

- جميع الـ Endpoints محمية بـ Authentication (عدا Health Check)
- بعض الـ Endpoints تحتاج صلاحيات Admin فقط
- بعض الـ Endpoints تحتاج صلاحيات Driver فقط
- Token يتم تحديثه تلقائياً بعد تسجيل الدخول
- يمكنك إنشاء Environment جديد للـ Production

## 🆘 حل المشاكل

### المشكلة: Token غير صالح
**الحل:** سجّل الدخول مرة أخرى من **Authentication > Login**

### المشكلة: 403 Forbidden
**الحل:** تأكد من أنك تستخدم حساب صحيح (Admin أو Driver)

### المشكلة: 404 Not Found
**الحل:** تأكد من أن `base_url` صحيح وأن الـ Server يعمل

### المشكلة: المتغيرات لا تعمل
**الحل:** تأكد من تفعيل Environment في أعلى يمين Postman

---

**تم إنشاء الملفات بواسطة:** Laravel Backend System
**التاريخ:** 2024

