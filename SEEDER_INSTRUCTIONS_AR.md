# تعليمات استيراد البيانات الوهمية (Faker Data Seeder)

## 📋 نظرة عامة

تم إنشاء ملف `FakerDataSeeder` الذي يقوم بإنشاء بيانات وهمية للقاعدة باستخدام مكتبة Faker، مع صور مجانية من Unsplash.

## 📦 البيانات التي سيتم إنشاؤها

- ✅ **1 مستخدم إداري** (Admin)
- ✅ **8 فئات** (Categories) بأسماء عربية
- ✅ **50 منتج** (Products) مع صور من Unsplash
- ✅ **10 سائقين** (Drivers)
- ✅ **مخزون المستودع** لكل منتج
- ✅ **مخزون السائقين** (توزيع عشوائي)
- ✅ **30 عملية بيع** (Sales) مع فواتير

## 🚀 طريقة الاستيراد

### الطريقة الأولى: استخدام الأمر المباشر

افتح Terminal أو Command Prompt في مجلد المشروع وقم بتنفيذ:

```bash
php artisan db:seed --class=FakerDataSeeder
```

### الطريقة الثانية: استخدام DatabaseSeeder الرئيسي

قم بتنفيذ:

```bash
php artisan db:seed
```

سيقوم هذا الأمر بتشغيل `DatabaseSeeder` الذي يستدعي `FakerDataSeeder` تلقائياً.

## ⚠️ ملاحظات مهمة

### قبل التشغيل:

1. **تأكد من إنشاء قاعدة البيانات:**
   - افتح phpMyAdmin
   - أنشئ قاعدة بيانات باسم `inventory`

2. **تأكد من تشغيل Migrations أولاً:**
   ```bash
   php artisan migrate
   ```

3. **إذا كنت تريد إعادة ملء البيانات:**
   ```bash
   php artisan migrate:fresh --seed
   ```
   ⚠️ **تحذير:** هذا الأمر سيحذف جميع البيانات الموجودة!

### بعد التشغيل:

ستحصل على:
- مستخدم إداري: `admin@inventory.com` / كلمة المرور: `password`
- 10 سائقين ببيانات وهمية (كلمة المرور لجميعهم: `password`)
- 50 منتج مع صور من Unsplash
- بيانات بيع وهمية

## 🔍 التحقق من البيانات

يمكنك التحقق من البيانات المستوردة عبر:

1. **phpMyAdmin:**
   - افتح http://localhost/phpmyadmin
   - اختر قاعدة البيانات `inventory`
   - تصفح الجداول

2. **API:**
   ```bash
   # تسجيل الدخول كإداري
   POST http://localhost:8000/api/login
   {
       "email": "admin@inventory.com",
       "password": "password"
   }
   
   # الحصول على المنتجات
   GET http://localhost:8000/api/products
   Authorization: Bearer {your_token}
   
   # الحصول على الإحصائيات
   GET http://localhost:8000/api/admin/stats
   Authorization: Bearer {your_token}
   ```

## 🖼️ الصور من Unsplash

جميع المنتجات تحتوي على روابط صور مجانية من Unsplash. الصور متاحة للاستخدام التجاري وغير التجاري.

## 🔄 إعادة تشغيل Seeder

إذا أردت إعادة ملء البيانات:

```bash
# حذف جميع البيانات وإعادة إنشائها
php artisan migrate:fresh --seed

# أو تشغيل Seeder محدد فقط
php artisan db:seed --class=FakerDataSeeder
```

## 📝 ملاحظات إضافية

- جميع كلمات المرور للبيانات الوهمية هي: `password`
- الصور من Unsplash قد تحتاج اتصال بالإنترنت للتحميل
- البيانات الوهمية يتم إنشاؤها بشكل عشوائي في كل مرة
- يمكنك تعديل عدد المنتجات أو السائقين في ملف `FakerDataSeeder.php`

## 🆘 حل المشاكل

### خطأ: "Class FakerDataSeeder not found"
```bash
composer dump-autoload
php artisan db:seed --class=FakerDataSeeder
```

### خطأ: "Database connection failed"
- تأكد من إعدادات `.env`
- تأكد من تشغيل MySQL في XAMPP
- تأكد من إنشاء قاعدة البيانات `inventory`

### خطأ: "Table doesn't exist"
```bash
php artisan migrate
php artisan db:seed --class=FakerDataSeeder
```

---

**تم إنشاء الملف بواسطة:** Laravel Backend System
**التاريخ:** 2024

